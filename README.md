
  # Estimate Generation Program

  This is a code bundle for Estimate Generation Program. The original project is available at https://www.figma.com/design/cdDWPA5ZxOScxDlecbZnOM/Estimate-Generation-Program.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  